#include "bits/stdc++.h"

using namespace std;

class Solution {
public:
    unordered_map<char, vector<char>> {
        {'1', {}},
        {'2', {'a', 'b', 'c'}},
        {'3', {'d', 'e', 'f'}},
        {'4', {'g', 'h', 'i'}},
        {'5', {'j', 'k', 'l'}},
        {'6', {'m', 'n', 'o'}},
        {'7', {'p', 'q', 'r', 's'}},
        {'8', {'t', 'u', 'v'}},
        {'9', {'w', 'z', 'y', 'z'}}
    };

    vector<string> letterCombinations(string digits) {
        vector<string> res;



        return res;
    }
};